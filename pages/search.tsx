import { Card } from "@mantine/core";
import { SitePage } from "types";

const SearchPage: SitePage = () => {
  return (
    <Card radius="md">
      Ця сторінка відображає результати пошуку. Зайдіть пізніше.
    </Card>
  );
};

export default SearchPage;
